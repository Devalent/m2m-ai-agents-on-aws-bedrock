/**
 * Foundation model ID.
 */
export const foundationModel = 'anthropic.claude-instant-v1';

/**
 * Idle session timeout in seconds.
 */
export const sessionTimeout = 60;

/**
 * Enable agent tracing.
 */
export const useTracing = true;
